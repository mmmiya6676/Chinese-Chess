# 中国象棋 Qt C++ 课程大作业 —— 答辩 PPT 完整文稿

---

## 第 1 页：封面

**大标题：** 中国象棋 —— 基于 Qt 6.11 与 C++17 的传统棋类游戏开发

**副标题：** 中国象棋课程大作业 · 答辩汇报

**信息栏：**
- 姓名：[你的姓名]
- 学号：[你的学号]
- 班级：[你的班级]
- 指导教师：[教师姓名]
- 答辩日期：2026 年 X 月 X 日

**口述话术：** "各位老师好，我叫 XXX，学号 XXX。我的课程大作业题目是《中国象棋——基于 Qt 6.11 与 C++17 的传统棋类游戏开发》。本项目的目标是将中国传统象棋游戏从控制台迁移到现代化 GUI 图形界面，并加入 AI 人机对战功能。接下来我将从选题背景、系统架构、核心技术和开发心得四个方面进行汇报。"

**切换效果：** 平滑淡入淡出（1.5 秒）
**元素动画：** 大标题缩放浮现（0.8 秒）→ 副标题逐字淡入（0.5 秒）→ 信息栏逐行滑入（每行 0.3 秒）

---

## 第 2 页：选题背景与需求分析

**大标题：** 选题背景与需求分析

**正文要点：**
- 中国象棋作为国家级非物质文化遗产，具有深厚的文化底蕴
- 市面上象棋软件多为商业产品（如天天象棋、腾讯象棋），代码不透明
- 课程要求掌握 C++ 面向对象编程、Qt GUI 开发、文件 I/O、算法设计等核心技能
- 本课题完美覆盖上述教学大纲要求，兼具实用性与技术深度

**需求分析：**
- 双人对战：支持鼠标点击走棋 + 传统中文记谱法输入
- 人机对战：AI 引擎采用博弈树搜索，可调难度
- 游戏管理：存档/读档（含步法回放）、排行榜、悔棋/重做
- 用户体验：古风木色 UI、楷体字、90 秒倒计时、走法高亮提示
- 跨版本：控制台版（Windows 控制台 API）+ Qt GUI 版

**口述话术：** "在选题阶段，我考虑了几个方向，最终选择了中国象棋。一方面，象棋逻辑规则清晰但实现有挑战性——7 种棋子各有不同的走法，还有将军、将死、飞将等特殊规则。另一方面，这个课题前后端分离度高，可以同时锻炼我的 C++ 算法能力和 Qt GUI 开发能力。我从四个维度进行了需求梳理：双人对战核心功能、人机 AI 对战、游戏管理辅助功能，以及 UI 交互体验。"

**切换效果：** 平滑推进（0.8 秒）
**元素动画：** 正文四要点逐行滑入（每行 0.4 秒）→ 需求分析五要点小圆点 + 文字依次淡入（每项 0.3 秒）

---

## 第 3 页：开发环境与 Qt 技术栈

**大标题：** 开发环境与技术栈

**正文要点：**

| 层级 | 技术选择 | 说明 |
|------|----------|------|
| 语言 | C++17 | 使用智能指针、lambda、optional 等现代特性 |
| GUI 框架 | Qt 6.11.0 | 使用 QtWidgets 模块（非 QML） |
| 编译器 | MinGW-w64 GCC 16.1.0 | 通过 MSYS2 包管理器安装 |
| 构建系统 | qmake + mingw32-make | .pro 工程文件管理 |
| IDE | Qt Creator 19.0 + VS Code | Qt Creator 负责 UI 调试，VS Code 写 C++ 逻辑 |
| 版本控制 | Git + GitHub | 控制台版 (master) 和 Qt 版 (qt-gui) 分别管理 |

**核心 Qt 技术点：**
- QPainter 绘制引擎：棋盘网格线、棋子圆形渐变、文字居中、抗锯齿
- QTimer 定时器：每秒倒计时 + singleShot 延迟触发
- 信号与槽：BoardWidget ↔ MainWindow 解耦通信
- QSS 样式表：全局统一古风配色
- 模态/非模态对话框：QDialog::exec() vs QWidget::show()
- QMainWindow + 嵌套布局：QVBoxLayout / QHBoxLayout / QFormLayout

**口述话术：** "开发环境方面，我选择了 MSYS2 + MinGW GCC 16.1 作为编译工具链，Qt 6.11 作为 GUI 框架。选择 Qt 而不是 MFC 或 WinForm 的原因是：Qt 跨平台、文档完善、信号槽机制清晰。我用 qmake 管理工程文件，Qt Creator 做 UI 调试，VS Code 写 C++ 逻辑部分——两个 IDE 同时打开同一个项目文件夹，各取所长。技术栈从左到右覆盖了从底层编译器到上层 UI 框架的完整工具链。"

**切换效果：** 平滑淡入淡出（1 秒）
**元素动画：** 表格整体滑入（0.6 秒）→ 下方技术点列表逐行淡入（每行 0.3 秒）

---

## 第 4 页：系统整体架构设计

**大标题：** 系统整体架构设计

**正文要点（架构图）：**

```
┌─────────────────────────────────────────────┐
│                UI 层 (Qt Widgets)            │
│  ┌──────────┐ ┌──────────┐ ┌─────────────┐ │
│  │ BoardWidget │ MainWindow │ Dialogs × 5  │ │
│  └──────────┘ └──────────┘ └─────────────┘ │
├─────────────────────────────────────────────┤
│              游戏逻辑层 (纯 C++)             │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌───────────┐ │
│  │ Game │ │Board │ │Chess │ │SaveManager│ │
│  │      │ │10×9  │ │Piece │ │存档/排行   │ │
│  └──────┘ └──────┘ └──────┘ └───────────┘ │
├─────────────────────────────────────────────┤
│              AI 引擎层 (博弈树搜索)           │
│  ┌──────────────────────────────────────┐   │
│  │  ChessAI: Minimax + Alpha-Beta 剪枝  │   │
│  │  评估函数: 棋子价值表 (将=10000...)    │   │
│  └──────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

**分层设计思想：**
- UI 层只负责显示和事件转发，不包含任何游戏规则逻辑
- 游戏逻辑层是纯 C++ 代码，不依赖任何 Qt 头文件，可独立编译测试
- AI 层通过 ChessPiece 的 getValidMoves() 接口获取候选走法，与游戏逻辑解耦

**类关系（核心继承树）：**
```
ChessPiece (抽象基类，定义 getValidMoves/getSymbol 纯虚接口)
  ├── King      (将/帅: 九宫格内横竖一步)
  ├── Advisor   (士/仕: 九宫格内斜走一步)
  ├── Elephant  (象/相: 田字走法，不能过河)
  ├── Knight    (马: 日字走法，蹩脚限制)
  ├── Rook      (车: 横竖不限步数)
  ├── Cannon    (炮: 平移不限 / 隔一子吃)
  └── Pawn      (兵/卒: 未过河只能前进，过河可左右)
```

**口述话术：** "系统采用经典的三层架构——UI 层、游戏逻辑层、AI 引擎层。UI 层全部用 Qt Widgets 实现，负责棋盘绘制和用户交互，但绝不触碰任何游戏规则代码。游戏逻辑层是纯 C++，Game 类管理整个游戏状态机——当前走棋方、步数、悔棋栈、将军检测等。Board 类维护 10×9 的棋子数组，ChessPiece 抽象基类定义了 getValidMoves 纯虚接口，7 种棋子各自实现自己的走法规则——这是典型的策略模式。AI 层通过调用 Game 类的接口仿真走棋，用 Minimax + Alpha-Beta 搜索最优解。"

**切换效果：** 平滑推进（1 秒）
**元素动画：** 架构图按三层从上到下依次滑入（每层 0.5 秒），类继承树从 ChessPiece 开始逐层展开（0.8 秒）

---

## 第 5 页：分功能模块详解（一）—— 核心游戏逻辑

**大标题：** 分功能模块详解 —— 核心游戏逻辑

**正文要点：**

**1. Game 类 —— 游戏状态机**
- `makeMove(from, to)`: 校验走法合法性 → 执行移动 → 切换走棋方 → 检测将死
- `undo()` / `redo()`: 使用 `std::stack<Move>` 记录历史走法，支持无限悔棋
- 中文记谱法解析（炮二平五 → 坐标转换，适配红黑方位差异）
- `wouldKingBeInCheck()`: 模拟走棋后检查己方将帅安全（防止"送将"违规）

**2. Board 类 —— 棋盘管理**
- `m_grid[10][9]` 二维指针数组存储棋子
- `isKingInCheck()`: 遍历对方所有棋子，判断是否能攻击到己方将/帅
- `isCheckmate()`: 遍历所有存活棋子的所有走法，无一能解除将军 → 将死
- 深拷贝语义（Ctrl+C/V 构造），管理棋子对象生命周期

**3. ChessPiece 抽象类 —— 策略模式**
- 纯虚函数 `getValidMoves(board)` 由 7 个子类分别实现
- 每个棋子知道自己的移动规则，但不关心"走后会不会被将军"（由 Game 类过滤）
- `operator<` 按棋子价值排序，用于走法优先级

**口述话术：** "游戏逻辑部分最核心的是三个类：Game-状态机、Board-棋盘、ChessPiece-棋子。Game 类是我设计的工作量最大的类——它管理完整的游戏状态转换，包括走法校验、将军将死检测、悔棋红做、存档读档。Board 类负责 10×9 的网格管理和将军检测算法。ChessPiece 用了策略模式——基类定义接口，7 个子类各自实现独特的走棋规则，这样一个车不需要知道马怎么走。"

**切换效果：** 平滑淡入淡出（1 秒）
**元素动画：** 三个模块标题依次滑入（每个 0.3 秒）→ 各子标题加粗后逐行淡入（每行 0.2 秒）

---

## 第 6 页：分功能模块详解（二）—— AI 人机对战

**大标题：** 分功能模块详解 —— AI 人机对战引擎

**正文要点：**

**算法选型：**
- 采用传统博弈树搜索（Minimax + Alpha-Beta 剪枝），不使用神经网络/强化学习
- 符合课程要求中"传统搜索算法"的规定

**四步流水线：**
1. **局面评估**：遍历 10×9 棋盘，累加 `红方棋子价值 - 黑方棋子价值`
   - 将/帅 = 10000、车 = 500、马/炮 = 300、士/象 = 100、兵/卒 = 70
   - AI 执黑 → 选最低分走法（Min 层）
2. **合法走法生成**：遍历同色棋子 → `getValidMoves()` → `wouldKingBeInCheck()` 过滤
3. **Minimax 递归搜索**：Max 层（红方）选最大分，Min 层（黑方）选最小分
4. **Alpha-Beta 剪枝**：当 `alpha >= beta` 时跳过后续分支，深度 3 时能剪掉约 60% 节点

**难度分级：**

| 难度 | 搜索深度 | 平均思考时间 |
|------|---------|-------------|
| 初级 | 1 层 | < 0.1 秒 |
| 中级 | 2 层 | 0.2~0.5 秒 |
| 高级 | 3 层 | 0.5~2 秒 |

**口述话术：** "AI 引擎是我这个项目最有技术含量的一块。我没有使用 AlphaZero 或神经网络那类黑盒方法，而是选择传统的博弈树搜索——Minimax 加 Alpha-Beta 剪枝。核心思路是：把游戏看成一棵树，每层交替代表双方走棋，用棋子价值表评估叶子节点，然后 Min 和 Max 交替选优。Alpha-Beta 剪枝的关键在于它不会改变最终结果，但能砍掉大量无用分支——在深度 3 搜索时大约能剪掉 60% 的节点，响应时间从 5 秒以上降到 2 秒以内。"

**切换效果：** 平滑推进（0.8 秒）
**元素动画：** 四步流水线从左到右逐卡片滑入（每卡 0.4 秒）→ 难度表格整体淡入（0.5 秒）

---

## 第 7 页：分功能模块详解（三）—— GUI 界面与用户交互

**大标题：** 分功能模块详解 —— Qt GUI 界面与交互

**正文要点：**

**界面布局（MainWindow）：**

```
[设置]                              步数: 5
        黑方: 张三
        剩余 85 秒
   ┌──────────────────────┐
   │                      │
   │    棋盘 (QPainter)    │  ← BoardWidget (600×660 px)
   │                      │
   └──────────────────────┘
        剩余 80 秒
        红方: 李四
  [悔棋] [下一步] [提示] [投降]
```

**BoardWidget 核心实现：**
- `paintEvent()` 被 Qt 在窗口显示/刷新时自动调用，分三步绘制：网格线→高亮→棋子
- `QRadialGradient` 径向渐变模拟木质棋子立体感
- `QPainter::Antialiasing` 抗锯齿让圆和文字边缘平滑
- `mousePressEvent()` 实现两步走棋：第一次点选子，第二次点走子
- 合法走法高亮提示（绿色点 = 空位，红色圈 = 可吃子）

**Qt 核心技术展示：**
- 信号-槽：`BoardWidget::moveMade` → `MainWindow::onMoveMade` → 更新计时 + 步数
- QTimer 定时器：每秒递减倒计时，`singleShot` 延迟 AI 走棋
- QSS 样式表：统一古风木色 (#d4b896) 配色方案
- 模态/非模态对话框：初始菜单 (exec) vs 主窗口 (show+app.exec)
- 多种 QWidget 控件：QPushButton、QLabel、QLineEdit、QComboBox、QTableWidget、QListWidget

**口述话术：** "GUI 层是我花了最多时间调试的部分。棋盘绘制用的是 Qt 的 QPainter 引擎——画线、画圆、写文字、做渐变都靠它。一个关键设计是 paintEvent 的被动触发机制——我不是主动画，而是等着 Qt 来叫我画，这样可以避免不必要的重绘。鼠标交互用了两阶段模型：第一步选中棋子显示合法走法提示，第二步点击目标执行走棋。每个合法走法都经过了 wouldKingBeInCheck 过滤，保证界面提示和游戏逻辑完全一致。"

**切换效果：** 平滑淡入淡出（1 秒）
**元素动画：** 界面布局图先整体浮现（0.6 秒）→ BoardWidget 要点逐条滑入（每项 0.3 秒）→ Qt 技术列表逐行淡入（每行 0.2 秒）

---

## 第 8 页：核心重难点代码解析

**大标题：** 核心重难点代码解析

**正文要点：**

**1. Alpha-Beta 剪枝核心代码（伪代码标注）：**
```cpp
// Max 层 — 选择最大得分
for (const auto& mv : moves) {
    game.makeMove(mv.from, mv.to);
    int score = minimax(game, depth-1, false, alpha, beta);
    game.undo();                            // 回溯：必须还原棋盘状态
    if (score > maxScore) maxScore = score;
    if (maxScore > alpha) alpha = maxScore; // 刷新已知最好下界
    if (alpha >= beta) break;               // 剪枝！跳过无用分支
}
```

**2. 信号-槽连接机制（代码展示）：**
```cpp
// BoardWidget 声明信号
signals: void moveMade(); void gameOverSignal();

// BoardWidget 发射信号（走棋后）
emit moveMade();   // 通知 MainWindow "走了一步棋"

// MainWindow 连接信号到槽
connect(m_board, &BoardWidget::moveMade,
        this, &MainWindow::onMoveMade);

// onMoveMade: 重置计时 → 如果 AI 模式则延迟触发 AI
void MainWindow::onMoveMade() {
    resetTimer();
    if (m_aiMode && game->getCurrentPlayer() == BLACK)
        QTimer::singleShot(300, this, &MainWindow::doAIMove);
}
```

**3. QPainter 棋子绘制（径向渐变立体效果）：**
```cpp
QRadialGradient grad(center, PIECE_R);
grad.setColorAt(0, QColor(255, 245, 220));  // 圆心 = 亮米色
grad.setColorAt(1, QColor(200, 160, 100));  // 边缘 = 暗木色
p.setBrush(grad);
p.drawEllipse(QPointF(center), PIECE_R, PIECE_R);  // 画圆
p.drawText(QRect(...), Qt::AlignCenter, text);       // 居中写中文
```

**口述话术：** "我选取三段最有代表性的代码展示。第一段是 AI 的 Alpha-Beta 剪枝循环——关键在 `alpha >= beta` 的判断和 `undo()` 的状态回溯。第二段是 Qt 信号槽机制——BoardWidget 只负责发射 '走了一步棋' 的信号，至于更新计时、触发 AI 这些后续操作全都由 MainWindow 的槽函数完成，两者完全解耦。第三段是棋子绘制的径向渐变——用中心亮、边缘暗的色阶模拟木制棋子的立体感，加上抗锯齿和楷体字，整体效果古朴自然。"

**切换效果：** 平滑推进（1 秒）
**元素动画：** 三段代码依次缩放浮现（每段 0.6 秒，动画带轻微弹出效果）→ 口述要点同步逐行淡入

---

## 第 9 页：程序运行演示效果

**大标题：** 程序运行演示效果

**正文要点（配合截图/视频展示）：**

**截图清单：**

| 编号 | 截图内容 | PPT 插入页码 |
|------|---------|-------------|
| ① | 初始菜单界面 | 第 9 页 |
| ② | 新建游戏弹窗（输入名字 + 选 AI 模式） | 第 9 页 |
| ③ | 游戏主界面（完整棋盘 + 计时器 + 按钮） | 第 9 页 |
| ④ | 棋子选中高亮（黄色选中 + 绿色目标提示） | 第 9 页 |
| ⑤ | 设置下拉菜单（保存/排行榜/退出） | 第 9 页 |
| ⑥ | 排行榜表格界面 | 第 9 页 |
| ⑦ | 将死提示弹窗 | 第 9 页 |
| ⑧ | AI 走棋过程（中级难度对局） | 第 9 页 |

**视频播放：** 2 段视频，合计约 2 分钟（将在答辩现场配合讲解播放）

**口述话术：** "接下来我播放一段约 2 分钟的实际运行视频，展示从启动到完整的对局流程。包括：初始菜单→新建 AI 对战→走棋高亮→AI 回应→将死提示→排行榜记录。请大家观看。" [播放视频]

**切换效果：** 平滑淡入淡出（1.5 秒）
**元素动画：** 截图 4 幅一组按田字排列滑入（0.8 秒）→ 其余依次淡入

---

## 第 10 页：功能测试结果

**大标题：** 功能测试结果

**正文要点：**

**测试覆盖范围：**

| 测试类别 | 测试项 | 结果 |
|----------|--------|------|
| 走棋规则 | 7 种棋子各自的所有合法走法 | ✓ 全部通过 |
| 特殊规则 | 将军检测、将死判定、飞将检测 | ✓ 全部通过 |
| 悔棋/重做 | 连续悔棋 10 步后的状态校验 | ✓ 棋盘与步数完全一致 |
| 存档/读档 | 保存 → 删除当前局 → 读档 → 步法回放 | ✓ 含步法记录，读档后可悔棋 |
| AI 对战 | 三档难度各 5 局，共 15 局测试 | ✓ AI 无非法走法，无超时 |
| 计时器 | 人为等待 90 秒超时 + AI 思考计时扣除 | ✓ 超时判负正常 |
| 排行榜 | 胜负记录写入 + 读取 + 排名截图 | ✓ 记录正确 |
| UI 交互 | 鼠标点击、按钮、菜单、弹窗 | ✓ 无卡顿、无崩溃 |

**历史 Bug 修复：** 累计修复 30+ 个 Bug，涵盖走法逻辑、存档兼容性、控制台模式切换、计时同步、胜负记录倒置等。详见 `bug-fix-log.txt`。

**口述话术：** "在开发过程中，我对每个功能模块都进行了系统测试。这里有 8 个核心测试类别共 30 多项具体测试。特别提一下——最早的控制台版本我累计修复了 30 多个 Bug，都记录在代码仓库的 bug-fix-log 中。这些 Bug 涵盖从 C++ 基础语法错误（如逻辑运算符优先级）到复杂的控制台 API 模式冲突，每个都有详细的原因分析和解决方案记录。"

**切换效果：** 平滑推进（0.8 秒）
**元素动画：** 表格行交替从左右滑入（0.6 秒）→ 历史修复数字放大浮现（0.5 秒）

---

## 第 11 页：开发难点与解决思路

**大标题：** 开发难点与解决思路

**正文要点：**

**难点 1：MSYS2 GCC 在 VS Code/cmake 中无法直接编译**
- 问题：MinGW GCC 有内部依赖 DLL（cc1plus 寻找 libisl-23.dll 等），脱离 MSYS2 bash 环境运行时找不到
- 解决：① 用 `.bat` wrapper 脚本注入 MSYS2 PATH；② 后来放弃 VS Code cmake，改用 Qt Creator + qmake 构建

**难点 2：控制台鼠标输入与 IME 中文输入法的冲突**
- 问题：`ReadConsoleInput` 需要关闭 `ENABLE_LINE_INPUT` 才能实时读取鼠标事件，但 IME 输入法又需要 LINE_INPUT 模式
- 解决：设计"子模式切换"——鼠标模式默认关闭 LINE_INPUT，按 K 键或右键时临时切回 LINE_INPUT + getline，输入结束后切回

**难点 3：AI 思考阻塞事件循环导致计时器停止**
- 问题：Minimax 递归是同步的，期间 onTimerTick() 不会被调用，AI 的倒计时显示卡住
- 解决：在 AI 思考前后记录 `std::chrono::steady_clock::now()`，算出差值手动从计时器扣除

**难点 4：存档文件名中文编码问题**
- 问题：Windows 下中文文件名在不同编码环境下可能乱码或无法创建
- 解决：建立玩家 ID 注册表（`players.dat`），存档文件用纯 ASCII 命名（`1_vs_2_play1.txt`），读档时反向翻译为中文名展示

**口述话术：** "在开发过程中我遇到了几个印象深刻的难点。第一个是工具链兼容性问题——MSYS2 的 GCC 需要特定的运行时环境，我花了将近两天反复调试才找到正确的编译方式。第二个是控制台的鼠标和中文输入法的冲突——Windows 控制台 API 很底层，两种模式互斥，我用了一个巧妙的'子模式切换'方案。第三个是 AI 阻塞事件循环导致计时器冻结——用 chrono 记录思考耗时手动扣除。这些坑虽然花了不少时间，但也让我真正理解了底层机制是怎样运作的。"

**切换效果：** 平滑推进（0.8 秒）
**元素动画：** 四个难点卡片依次从下方滑入（每个 0.5 秒，带有缓入缓出）→ 问题描述先出现 → 解决方案跟随淡入

---

## 第 12 页：项目总结与拓展展望

**大标题：** 项目总结与拓展展望

**正文要点：**

**项目总结：**
- 完成两大版本：Windows 控制台版 + Qt GUI 版，代码复用率 > 80%
- 总代码量：约 5000 行 C++ 代码，40+ 个源文件
- 实现全部设计目标：完整象棋规则、AI 引擎、存档系统、排行榜
- 核心收获：面向对象设计、Qt 信号槽机制、博弈树搜索、控制台 API、Git 版本管理
- 通过 30+ 个 Bug 的排查和修复，深入理解了 C++ 编译器行为、UTF-8 编码、Windows 控制台底层机制

**拓展方向：**
1. 网络对战：利用 Qt 的 QTcpSocket 实现局域网/远程对战
2. AI 增强：加入迭代加深搜索、置换表缓存、开局库
3. 移动端移植：利用 Qt 的跨平台特性编译到 Android/iOS
4. 棋谱格式支持：支持 PGN 标准格式导入/导出
5. 深度学习 AI：使用 TensorFlow Lite 集成神经网络评估函数

**致谢：** 感谢指导教师 X 老师在选题和技术路线上的指导。感谢课程助教在答辩准备中的帮助。

**口述话术：** "最后做个总结。这个项目总共约 5000 行 C++ 代码，开发了两个版本——控制台版和 Qt GUI 版。我觉得这个过程最有价值的不是最后这个软件本身，而是那些踩过的坑——编译器兼容性、控制台 API、信号槽机制、AI 剪枝逻辑——每个坑都是一次深入理解底层原理的机会。代码全部开源在 GitHub 上。如果有更多时间，我有五个拓展方向想继续做：网络对战、AI 增强、移动端移植、棋谱格式和深度学习集成。以上就是我的答辩汇报，请各位老师提问，谢谢。"

**切换效果：** 平滑淡入淡出（1.5 秒）
**元素动画：** "致谢"幻灯片风格收尾 —— 所有元素整体淡出 50% → 致谢文字从中央缩放浮现（1 秒）

---

## 附录：完整截图清单

| PPT 页码 | 截图文件 | 操作步骤 | 展示重点 |
|----------|---------|----------|---------|
| 第 9 页 | `棋盘展示.png` | 启动 Qt 版 → 新建游戏 → 走几步棋 | 完整棋盘、棋子、古风 UI 配色 |
| 第 9 页 | `初始界面.png` | 启动程序 | 初始菜单四个按钮 + 深木色调 |
| 第 9 页 | `新建游戏界面.png` | 初始菜单 → 新建游戏 | 玩家名输入 + AI 勾选框 + 难度下拉 |
| 第 9 页 | `设置列表.png` | 游戏中 → 点击设置按钮 | 下拉菜单：保存/排行榜/退出 |
| 第 9 页 | `走法显示.png` | 选中一个棋子 | 黄色选中高亮 + 绿色/红色走法提示 |
| 第 9 页 | `将死提示.png` | 将死对手 | 游戏结束弹窗 |
| 第 9 页 | `排行榜功能.png` | 初始菜单 → 排行榜 | 表格：排名/玩家/胜场/胜率 |
| 第 9 页 | `类图.png` | （独立图表）| UML 类继承关系：ChessPiece → 7 子类 |
| 第 9 页 | `程序展示.mp4` | 完整对局流程（1 分 12 秒） | 初始→新建→走棋→AI→结束 |
| 第 9 页 | `chess.exe 2026-05-25 17-00-52.mp4` | 控制台完整对局（1 分 59 秒） | 控制台鼠标走棋 + 键盘记谱法 + 存档 |

---
